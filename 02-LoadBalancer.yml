apiVersion: v1
kind: pod
metadata:
  name: myapp-pod
  labels: 
    app: myapp
spec:
  containers: 
   - name: myapp-container
     image: stacksimplify/kubenginx:1.0.0
     ports: 
      - containerPort: 80
